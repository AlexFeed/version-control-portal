{
  "name": "version-control-portal",
  "version": "1.0.0",
  "description": "Root package for Version Control Portal",
  "scripts": {
    "dev:backend": "cd backend && npm run backend:dev",
    "dev:frontend": "cd frontend && npm run dev",
    "all:dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
